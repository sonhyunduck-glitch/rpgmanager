/* =========================================================
   ROTATE OVERLAY — 비활성 (세로 모드 전환 완료)
   모바일은 세로 모드로 동작하므로 orientation lock 불필요.
   ========================================================= */

export default function RotateOverlay() {
  return null;
}
